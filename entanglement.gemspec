Gem::Specification.new do |gem|
  gem.authors       = ['spannerinworks']
  gem.summary       = 'A gem for tracking message calls between objects so that they can be visualised'
  gem.files         = ['lib/entanglement.rb']
  gem.name          = 'entanglement'
  gem.require_paths = ['lib']
  gem.version       = '0.0.1'
end
